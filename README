This is Perl module Cache::Memcached::Fast::Safe.

INSTALLATION

Cache::Memcached::Fast::Safe installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan Cache::Memcached::Fast::Safe

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

Cache::Memcached::Fast::Safe documentation is available as in POD. So you can do:

    % perldoc Cache::Memcached::Fast::Safe

to read the documentation online with your favorite pager.

Masahiro Nagano
